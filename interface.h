#include "definitions.h"

//queue functions

void InitializeQueue(Queue*);
int Empty(Queue*);
void Insert(process, Queue*);
void Remove(Queue*, process* );

//semaphore functions

void copy(semaphore* , process*);
void copy_i(semaphore* , process*);

